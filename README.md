# webload-multiscript-auditor
WPF app that extracts transaction data from Webload (.wlp) project files and/or its own dedicated SQL Server DB to create an audit log on any detected changes to HTTP requests
