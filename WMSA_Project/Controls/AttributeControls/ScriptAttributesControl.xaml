<UserControl x:Class="WMSA_Project.Controls.AttributeControls.ScriptAttributesControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WMSA_Project.Controls.AttributeControls"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="500">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>  
    </UserControl.Resources>
    <Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" x:Name="Text_Block" HorizontalAlignment="Left" Margin="15,0,0,0.4" Text="{Binding SelectedValue}"/>
            <Button Grid.Column="1" x:Name="Btn_Select" Content="Select" Click="Select_Button_Click"/>
            <Button Grid.Column="2" x:Name="Btn_Add" Content="Add" Click="Add_Button_Click" Visibility="{Binding Path=AddButtonVisible, Converter={StaticResource BoolToVis}}"/>
        </Grid>
    </Grid>
</UserControl>
